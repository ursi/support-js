{
  "name": "@ursi/support",
  "version": "1.0.1-0",
  "description": "The JavaScript component to the SupPort Elm package",
  "main": "main/SupPort.js",
  "browser": "browser/index.js",
  "types": "main/index.d.ts",
  "scripts": {
    "test": "tsc --outDir test --module CommonJS && jest",
    "build": "tsc --outDir main --module CommonJS -d & tsc --outDir browser",
    "prepare": "tsc --outDir main --module CommonJS -d & tsc --outDir browser"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ursi/support-js.git"
  },
  "keywords": [
    "elm",
    "port",
    "support"
  ],
  "author": "Mason Mackaman",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ursi/support-js/issues"
  },
  "homepage": "https://github.com/ursi/support-js#readme",
  "devDependencies": {
    "jest": "^26.0.1"
  },
  "dependencies": {
    "typescript": "^3.9.5"
  }
}
